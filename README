#### Setup supabase

`$ npm i && cd supabase && npx supabase start`

Just for control, reset db

`$ npx supabase db reset`


#### Run FastAPI application

`$ docker-compose build --no-cache`

Bring up the container

`$ docker-compose up --build`

